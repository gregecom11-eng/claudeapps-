{
  "name": "sdluxury-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "MCP server that lets Claude.ai create and manage rides in the SDLuxury Operations dashboard.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "secret:set": "wrangler secret put"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
